<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Ege Kalyon Yazılım | Kurumsal Lojistik Teknolojileri</title>
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    
    
<style>
* {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
}
        :root {
            --idx-primary: #0071e3;
            --idx-primary-glow: rgba(0, 113, 227, 0.3);
            --idx-accent: #29dec8;
            --idx-bg: #000000;
            --idx-card-bg: rgba(15, 15, 15, 0.8);
            --idx-border: rgba(255, 255, 255, 0.08);
            --idx-glass: rgba(255, 255, 255, 0.03);
        }

        html { scroll-behavior: smooth; overflow-x: hidden; }
        body.idx-body {
            background-color: var(--idx-bg);
            color: #ffffff;
            margin: 0; padding: 0;
            font-family: 'Inter', -apple-system, sans-serif;
            overflow-x: hidden;
            width: 100%;
        }

        /* DİNAMİK ARKA PLAN */
        .idx-glow-wrap { position: fixed; top: 0; left: 0; width: 100%; height: 100%; z-index: -1; overflow: hidden; pointer-events: none; }
        .idx-glow-1 { position: absolute; width: 150%; height: 60%; background: var(--idx-primary); filter: blur(150px); opacity: 0.1; top: -10%; right: -25%; }
        .idx-glow-2 { position: absolute; width: 150%; height: 60%; background: var(--idx-accent); filter: blur(150px); opacity: 0.05; bottom: -10%; left: -25%; }

        /* NAVBAR - MOBİL UYUMLU */
        .idx-nav { position: fixed; top: 0; left: 0; width: 100%; z-index: 1000; padding: 15px 0; transition: 0.4s; }
        .idx-nav.scrolled { background: rgba(0,0,0,0.9); backdrop-filter: blur(20px); padding: 10px 0; border-bottom: 1px solid var(--idx-border); }
        .idx-nav-container { max-width: 1300px; margin: 0 auto; display: flex; justify-content: space-between; align-items: center; padding: 0 20px; }
        
        .idx-logo { display: flex; align-items: center; gap: 8px; font-weight: 900; font-size: 1.1rem; color: #fff; text-decoration: none; }
        .idx-logo i { color: var(--idx-primary); }
        
        .idx-nav-links { display: flex; align-items: center; gap: 8px; }
        .idx-nav-item { display: none; } /* Masaüstü linkleri mobilde gizle */
        
        .idx-btn-start { 
            background: var(--idx-primary); 
            color: #fff !important; 
            text-decoration: none; 
            padding: 8px 16px; 
            border-radius: 10px; 
            font-weight: 800; 
            font-size: 0.75rem; 
            box-shadow: 0 5px 15px var(--idx-primary-glow); 
        }

        /* HERO SECTION */
        .idx-hero { min-height: 100vh; display: flex; align-items: center; justify-content: center; text-align: center; padding: 120px 20px 60px; }
        .idx-hero-inner { width: 100%; max-width: 1100px; }
        
        .idx-badge { 
            display: inline-flex; align-items: center; gap: 8px; 
            background: rgba(255,255,255,0.03); border: 1px solid var(--idx-border); 
            padding: 8px 20px; border-radius: 100px; color: var(--idx-accent); 
            font-size: 0.7rem; font-weight: 800; margin-bottom: 25px; 
            text-transform: uppercase; letter-spacing: 1px;
        }

        .idx-hero h1 { 
            font-size: clamp(2.2rem, 8vw, 6rem); 
            font-weight: 900; line-height: 1.1; 
            margin: 0 0 25px 0; letter-spacing: -2px; 
        }
        .idx-gradient { background: linear-gradient(180deg, #fff 40%, #555 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
        
        .idx-hero p { 
            color: #a1a1a6; font-size: clamp(1rem, 4vw, 1.4rem); 
            margin-bottom: 40px; line-height: 1.5; max-width: 800px; margin-left: auto; margin-right: auto; 
        }

        /* TECH GRID */
        .idx-tech-area { width: 100%; margin-bottom: 80px; }
        .idx-tech-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 10px; margin-bottom: 20px; }
        .idx-tech-card { 
            background: #0a0a0a; border: 1px solid var(--idx-border); border-radius: 15px; 
            padding: 15px; cursor: pointer; transition: 0.3s; 
            display: flex; flex-direction: column; align-items: center; gap: 10px; text-align: center;
        }
        .idx-tech-card i { font-size: 1.2rem; color: var(--idx-primary); }
        .idx-tech-card span { font-size: 0.7rem; font-weight: 900; }

        .idx-info-panel { 
            background: rgba(255,255,255,0.02); border: 1px solid var(--idx-border); 
            border-radius: 20px; padding: 30px 20px; text-align: left; 
            margin-top: 20px; display: none;
        }
        .idx-info-panel h4 { font-size: 1.2rem; margin-bottom: 10px; color: var(--idx-primary); }
        .idx-info-panel p { font-size: 0.95rem; line-height: 1.5; color: #a1a1a6; }

        /* BENTO GRID */
        .idx-bento-section { padding: 60px 20px; }
        .idx-bento-grid { display: flex; flex-direction: column; gap: 20px; }
        .idx-bento-card { 
            background: var(--idx-card-bg); border: 1px solid var(--idx-border); 
            border-radius: 30px; padding: 40px 30px; min-height: 250px;
            display: flex; flex-direction: column; justify-content: flex-end; 
            position: relative; overflow: hidden;
        }
        .idx-bento-card h3 { font-size: 1.6rem; margin-bottom: 15px; letter-spacing: -1px; }
        .idx-bento-card p { font-size: 1rem; color: #86868b; line-height: 1.5; }
        .idx-bg-icon { position: absolute; top: 20px; right: 20px; font-size: 3rem; opacity: 0.1; color: var(--idx-primary); }

        /* PRICING */
        .idx-pricing-section { padding: 80px 20px; }
        .idx-pricing-grid { display: flex; flex-direction: column; gap: 20px; }
        .idx-price-card { 
            background: #0a0a0a; border: 1px solid var(--idx-border); 
            padding: 40px 30px; border-radius: 30px; 
        }
        .idx-price-card.featured { border-color: var(--idx-primary); background: rgba(0,113,227,0.05); }
        .idx-price-card h4 { font-size: 1rem; color: #555; margin-bottom: 15px; }
        .idx-price-card .price { font-size: 2.5rem; font-weight: 900; margin-bottom: 25px; }
        
        .idx-price-list { list-style: none; padding: 0; margin-bottom: 30px; }
        .idx-price-list li { margin-bottom: 12px; font-size: 0.9rem; color: #888; display: flex; align-items: center; gap: 10px; }
        .idx-price-list li i { color: var(--idx-accent); }

        /* FOOTER */
        .idx-footer { padding: 80px 20px 40px; text-align: center; border-top: 1px solid #111; }
        .idx-footer p { font-size: 1.2rem; margin-bottom: 10px; font-weight: 900; }
        .idx-footer small { font-size: 0.6rem; color: #444; letter-spacing: 2px; }

        /* TABLET & DESKTOP DÜZENLEMELERİ (Responsive Geçişleri) */
        @media (min-width: 768px) {
            .idx-nav-item { display: inline-block; }
            .idx-tech-grid { grid-template-columns: repeat(4, 1fr); }
            .idx-bento-grid { display: grid; grid-template-columns: repeat(12, 1fr); }
            .idx-b-main { grid-column: span 8; }
            .idx-b-side { grid-column: span 4; }
            .idx-b-wide { grid-column: span 12; }
            .idx-pricing-grid { display: grid; grid-template-columns: repeat(3, 1fr); }
            .idx-nav-container { padding: 0 40px; }
            .idx-logo { font-size: 1.3rem; }
            .idx-btn-start { font-size: 0.85rem; padding: 10px 24px; }
        }
    </style>
</head>
<body class="idx-body">

    <div class="idx-glow-wrap">
        <div class="idx-glow-1"></div>
        <div class="idx-glow-2"></div>
    </div>

    <nav id="idx-navbar" class="idx-nav">
        <div class="idx-nav-container">
            <a href="/" class="idx-logo">
                <i class="fas fa-microchip"></i>
                <span>EGE KALYON <span>YAZILIM</span></span>
            </a>
            <div class="idx-nav-links">
                <a href="#idx-tech" class="idx-nav-item">Teknoloji</a>
                <a href="#idx-bento" class="idx-nav-item">Modüller</a>
                <a href="/giris" class="idx-btn-start">GİRİŞ YAP</a>
            </div>
        </div>
    </nav>

    <header class="idx-hero">
        <div class="idx-hero-inner">
            <div class="idx-badge" data-aos="fade-down">
                <i class="fas fa-rocket"></i> Lojistik 4.0 Hazır
            </div>
            <h1 data-aos="fade-up">Lojistikte <br><span class="idx-gradient">Dijital Çağ.</span></h1>
            <p data-aos="fade-up" data-aos-delay="100">
                Operasyonunuzu cebinize taşıyın. Akıllı takip ve raporlama ile firmanızı her yerden yönetin.
            </p>
            
            <div id="idx-tech" class="idx-tech-area" data-aos="zoom-in" data-aos-delay="200">
                <div class="idx-tech-grid">
                    <div class="idx-tech-card" onclick="toggleTech('nodejs', this)"><i class="fab fa-node-js"></i><span>NODE.JS</span></div>
                    <div class="idx-tech-card" onclick="toggleTech('saas', this)"><i class="fas fa-cloud"></i><span>BULUT</span></div>
                    <div class="idx-tech-card" onclick="toggleTech('arvento', this)"><i class="fas fa-satellite"></i><span>TAKİP</span></div>
                    <div class="idx-tech-card" onclick="toggleTech('security', this)"><i class="fas fa-user-shield"></i><span>GÜVENLİK</span></div>
                </div>
                <div id="techPanel" class="idx-info-panel">
                    <h4 id="techTitle"></h4>
                    <p id="techDesc"></p>
                </div>
            </div>
        </div>
    </header>

    <section id="idx-bento" class="idx-bento-section">
        <div class="idx-bento-grid">
            <div class="idx-bento-card idx-b-main" data-aos="fade-up">
                <i class="fas fa-microchip idx-bg-icon"></i>
                <h3>Dijital <br>Yönetim</h3>
                <p>Kağıt trafiğine son verin. Tüm süreci tek merkezden, gerçek zamanlı yönetin.</p>
            </div>
            
            <div class="idx-bento-card idx-b-side" data-aos="fade-up" data-aos-delay="100">
                <i class="fas fa-chart-line idx-bg-icon"></i>
                <h3>Anlık <br>Rapor</h3>
                <p>Verimlilik verilerine anında ulaşın, kararlarınızı hızlandırın.</p>
            </div>

            <div class="idx-bento-card idx-b-wide" data-aos="fade-up" data-aos-delay="200">
                <i class="fas fa-handshake-angle idx-bg-icon"></i>
                <h3>Müşteri Şeffaflığı</h3>
                <p>Müşterilerinize özel panellerle yüklerini izletin, markanıza olan güveni %100 artırın.</p>
            </div>
        </div>
    </section>

    <section id="idx-pricing" class="idx-pricing-section">
        <div class="idx-pricing-grid">
            <div class="idx-price-card" data-aos="fade-up">
                <h4>KOBİ</h4>
                <div class="price">₺3.450 <span>/ay</span></div>
                <ul class="idx-price-list">
                    <li><i class="fas fa-check"></i> 500 Sevkiyat</li>
                    <li><i class="fas fa-check"></i> Arvento API</li>
                    <li><i class="fas fa-check"></i> Email Destek</li>
                </ul>
                <a href="/giris" class="idx-btn-start" style="display:block; text-align:center;">Demo</a>
            </div>
            
            <div class="idx-price-card featured" data-aos="fade-up" data-aos-delay="100">
                <h4>PROFESYONEL</h4>
                <div class="price">₺7.200 <span>/ay</span></div>
                <ul class="idx-price-list">
                    <li><i class="fas fa-check"></i> Sınırsız Kayıt</li>
                    <li><i class="fas fa-check"></i> Luca Entegrasyon</li>
                    <li><i class="fas fa-check"></i> Müşteri Portalı</li>
                </ul>
                <a href="/giris" class="idx-btn-start" style="display:block; text-align:center;">Hemen Başla</a>
            </div>

            <div class="idx-price-card" data-aos="fade-up" data-aos-delay="200">
                <h4>ENTERPRISE</h4>
                <div class="price">TEKLİF</div>
                <ul class="idx-price-list">
                    <li><i class="fas fa-check"></i> Özel Sunucu</li>
                    <li><i class="fas fa-check"></i> White-Label</li>
                    <li><i class="fas fa-check"></i> 7/24 VIP Destek</li>
                </ul>
                <a href="https://www.egekalyon.com.tr" class="idx-btn-start" style="display:block; text-align:center; background:#fff; color:#000 !important;">Bize Ulaşın</a>
            </div>
        </div>
    </section>

    <footer class="idx-footer">
        <p>EGE KALYON YAZILIM</p>
        <small>Powered by Batuhan Yıldız Enterprise &copy; 2026</small>
    </footer>

    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script>
        AOS.init({ duration: 1000, once: true });

        const techContent = {
            nodejs: { t: 'Node.js', d: 'Binlerce veriyi aynı anda işleyen yüksek performanslı motor.' },
            saas: { t: 'SaaS Altyapısı', d: 'Her firma için tamamen izole edilmiş, güvenli bulut alanı.' },
            arvento: { t: 'Canlı Takip', d: 'Araçlarınızın konumunu doğrudan panelinizden izleyin.' },
            security: { t: 'Güvenlik', d: 'AES-256 askeri düzey şifreleme ile tam koruma.' }
        };

        function toggleTech(key, element) {
            const panel = document.getElementById('techPanel');
            const title = document.getElementById('techTitle');
            const desc = document.getElementById('techDesc');
            const cards = document.querySelectorAll('.idx-tech-card');

            if (element.classList.contains('active')) {
                panel.style.display = 'none';
                element.classList.remove('active');
            } else {
                cards.forEach(c => c.classList.remove('active'));
                title.innerText = techContent[key].t;
                desc.innerText = techContent[key].d;
                panel.style.display = 'block';
                element.classList.add('active');
            }
        }

        window.onscroll = function() {
            var nav = document.getElementById('idx-navbar');
            if (window.pageYOffset > 50) nav.classList.add("scrolled");
            else nav.classList.remove("scrolled");
        };
    </script>
</body>
</html>

